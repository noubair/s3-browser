import React, { useEffect, useState } from "react";
import "./CustomMenu.scss";
const CustomMenu = () => (
  <div className="CustomMenu">
      <div className = "MenuItem">custom menu item</div>
    </div> 
);

export default CustomMenu;
